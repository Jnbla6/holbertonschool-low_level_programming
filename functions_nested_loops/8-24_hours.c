#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - check of a number
 * @n: the number to check
 *
 * Return: last correct
 */
void jack_bauer(void)
{
int hour;
for (hour = 0; hour <= 23; hour++)
{
printf("%d\n", hour);
}
}
