#include "main.h"
#include <stdio.h>
/**
 * _abs - check of a number
 * @r: the number to check
 *
 * Return: r if n is positive -r if n is negative
 */
int _abs(int r)
{
if (r < 0)
{
return (-r);
}
else
{
return (r);
}
}
