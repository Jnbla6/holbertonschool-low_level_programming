#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * _isdigit - checks for uppercase character
 * @c: character to check
 * Return: 1 if c is uppercase, 0 otherwise
 */
int mul(int a, int b);
{
return (a * b);
}
